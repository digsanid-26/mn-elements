/**
 * MN Logolist Editor Script
 * Handles taxonomy filtering based on post type selection
 */

(function($) {
    'use strict';

    // This script helps filter taxonomy options in the Elementor editor
    // The taxonomy dropdown shows all taxonomies with their associated post types in parentheses
    // Example: "Categories (Posts)", "Product Categories (Products)"
    
    console.log('MN Logolist Editor Script Loaded');

})(jQuery);
